## Testing Strategy
**Mode:** full-tdd
**Rationale:** {Why this component has runtime behaviour worth asserting — be specific.}
**Framework:** {test framework and key libraries already in the repo}
**Test location:** `tests/{feature}/{layer}/`
**Commands:**
  - Run:      `{exact run command}`
  - Coverage: `{exact coverage command, or omit if not configured}`
**Done when:** All tests green. Coverage target met. No regressions in adjacent suites.
